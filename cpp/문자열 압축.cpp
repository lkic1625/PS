#include <string>
#include <vector>

using namespace std;

int solution(string s) {
	int cache[1000];
	memset(cache, 1, sizeof(cache));
	int answer = 0;
	for (auto itvl = s.size(); itvl > 0; itvl++) {
		for (int i = 0; i < s.size(); i++) {
			
		}
	}
	return answer;
}